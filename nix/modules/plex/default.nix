{
  services.plex = {
    enable = true;
    openFirewall = true;
  };

  services.ombi = {
    enable = true;
    openFirewall = true;
  };

  services.tautulli = {
    enable = true;
    openFirewall = true;
  };
}
