# Global OpenCode Instructions

- Always perform all development in a new git branch and a new worktree.
- Use the tools provided by the opencode-worktree-session plugin to manage your
  worktrees.
- Do not make changes directly on the default branch. Always create a new
  branch, then create a new worktree for that branch.
